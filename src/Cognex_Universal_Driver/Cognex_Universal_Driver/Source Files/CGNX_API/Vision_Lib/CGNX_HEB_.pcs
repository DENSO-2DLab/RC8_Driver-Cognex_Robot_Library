'!TITLE "Denso robot program"

#Include "..\CGNX_API.pcs"

'
'HandEye Calibration (Initialize)
'
Function CGNX_HEB(ByVal srvID As Integer, ByVal cam As Integer) As Variant
	Dim sendMsg As String
	Dim recvMsg As String

	sendMsg = Sprintf("HEB,%d", cam)
	recvMsg = CGNX_ExecuteCmd(srvID, sendMsg)
	CGNX_HEB = Split(recvMsg, ",")
End Function
